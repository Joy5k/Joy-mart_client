export enum tagTypes {
   product = 'product',
   booking = 'booking',
   admin = 'admin',
   user = 'user',
   category = 'category',
 
}

export const tagTypesList = [
   tagTypes.admin,
   tagTypes.user,
   tagTypes.product,
   tagTypes.booking
];